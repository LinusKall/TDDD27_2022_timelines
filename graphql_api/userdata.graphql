query Query {
    apiVersion
    userdata(id: 4, hashedPassword: "password") {
        id
        timelines {
            id
            color
            events {
                id
                timelineId
            }
            tasks {
                id
                timelineId
            }
        }
    }
}